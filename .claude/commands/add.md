Read the file `.claude/CLAUDE.md` in the current project. Analyze its existing content and sections to find the most relevant place to insert the following line:

$ARGUMENTS

Rules for placement:
- If there is an existing section whose topic clearly matches the line being added, append the line there.
- If no section is a clear match but the content relates to an existing section's theme, place it in the closest matching section.
- If no existing section is a reasonable fit, create a new section heading that clearly describes the topic and place the line under it at the most logical position in the file.
- If the file is empty or has no structure, just add the line as the first piece of content without a section heading.
- Preserve all existing content exactly; only add the new line (and a new section heading if one is needed).

After inserting, confirm what you added and where.
